<%-include ('../layouts/adminheader.ejs')%>


<div class="container">
    <h1 class="my-5">Edit Order Status</h1>
    <form action="" method="post">
      <div class="form-group">
        <label for="status">Status:</label>
        <select class="form-control" id="status" name="status">
          <option value="pending" <% if (orderData.status === 'pending') { %>selected<% } %>>Pending</option>
          <option value="processing" <% if (orderData.status === 'processing') { %>selected<% } %>>Processing</option>
          <option value="shipped" <% if (orderData.status === 'shipped') { %>selected<% } %>>Shipped</option>
          <option value="delivered" <% if (orderData.status === 'delivered') { %>selected<% } %>>Delivered</option>
          <option value="cancelled" <% if (orderData.status === 'cancelled') { %>selected<% } %>>Cancelled</option>
        </select>
      </div>
      <button type="submit" class="btn btn-primary">Save</button>
    </form>
    <%
    if(typeof message != 'undefined'){
     %>
     <p style="color: red;font-family: Georgia, 'Times New Roman', Times, serif;"><%=message%></p>
     <%
    }
    %>
  </div>

  <%-include ('../layouts/adminfooter.ejs')%>




  